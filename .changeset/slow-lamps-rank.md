---
"trageti": minor
---

Implement the v0.3 rev2 scorer contract: `RRFScorer` is now the default scorer,
`RetrievalScorer` requires `scoreBatch`, and the previous weighted-linear scorer
is available as `LinearScorer`.
