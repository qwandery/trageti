---
---

Release notes for `0.4.1-alpha.1` are recorded directly in `CHANGELOG.md`.
The package version is intentionally pinned to this exact alpha prerelease, so
this empty changeset prevents Changesets from proposing an unintended `0.5.0`
bump from already-consumed release notes.
