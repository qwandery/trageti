---
---

Release notes for `0.4.0-rev.0` are recorded directly in `CHANGELOG.md`.
The package version has already been set to the exact beta remediation version,
so this empty changeset prevents Changesets from proposing an accidental
`0.4.0-rev.1` bump while still documenting the release process decision.
